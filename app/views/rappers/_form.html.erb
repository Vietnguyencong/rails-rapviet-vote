<%= form_with(model: rapper, local: true, class:"form-inline") do |form| %>
  <% if rapper.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rapper.errors.count, "error") %> prohibited this rapper from being saved:</h2>

      <ul>
        <% rapper.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group mb-2">
    <%= form.label :lucky_number, class:"mr-2" %>
    <%= form.text_field :lucky_number,class: "form-control", placeholder:"Password" %>
  </div>

  <div class="actions">
    <%= form.submit "confirm", class:"btn btn-primary mb-2 ml-2"%>
  </div>
<% end %>

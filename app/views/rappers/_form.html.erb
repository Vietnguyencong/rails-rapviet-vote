<%= form_with(model: rapper, local: true, class:"form-inline mt-4") do |form| %>
  <% if rapper.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rapper.errors.count, "error") %> prohibited this rapper from being saved:</h2>

      <ul>
        <% rapper.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="form-group mb-2 mt-2 hidden">
    <%= form.hidden_field :lucky_number, value:"0" %>
  </div>

  <div class="actions mt-2">
    <%= form.submit "Quay Số May Mắn Của Bạn", class:"btn btn-light mb-2 ml-2"%> 
  </div>
  
<% end %>
